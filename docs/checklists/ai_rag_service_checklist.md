# AI RAG 서비스 체크리스트

참조 문서: `docs/checklists/answer_checklist.md`

정리 기준:
- 특정 도메인, 문서 유형, 데이터셋 이름은 제거합니다.
- 일반적인 AI + RAG 서비스에 공통으로 적용할 항목만 남깁니다.
- 구현, 평가, 개선활동, 보안, 개선 루프를 모두 포함합니다.

## 1. 데이터 및 지식베이스 준비

- [ ] 수집 대상 문서와 데이터 소스를 정의합니다.
- [ ] 최신성, 신뢰도, 접근권한 기준으로 데이터 소스를 분류합니다.
- [ ] 원문, 요약, 메타데이터, 출처 정보를 함께 저장합니다.
- [ ] 문서 청크 기준을 정의하고 일관되게 적용합니다.
- [ ] 중복 문서, 오래된 문서, 충돌 문서를 정리합니다.
- [ ] 색인 누락, 메타 누락, 청크 오매핑 같은 데이터 품질 이슈를 추적합니다.
- [ ] 버전 변경 시 재색인 정책을 정의합니다.

## 2. 검색 품질

- [ ] 기본 검색 전략을 정의합니다.
- [ ] Lexical, Vector, Hybrid 검색을 비교합니다.
- [ ] BM25, Dense Retrieval, Reranker 조합을 검증합니다.
- [ ] Top-K, Score Threshold, Weight 조합을 최적화합니다.
- [ ] Query Expansion 또는 Query Rewrite 적용 여부를 검토합니다.
- [ ] 정답 문서가 상위 랭킹에 진입하는지 점검합니다.
- [ ] 검색 실패 원인을 색인, 메타, 쿼리, 랭킹 단계로 분리합니다.
- [ ] 검색 지연시간과 비용을 함께 측정합니다.

## 3. 컨텍스트 구성

- [ ] 검색 결과를 그대로 넣을지, 정제한 컨텍스트만 넣을지 결정합니다.
- [ ] Context Refinement(컨텍스트 정제) 적용 여부를 검토합니다.
- [ ] 상위 1~N개 컨텍스트만 사용하는 방식의 성능을 비교합니다.
- [ ] 중복 청크, 노이즈 청크, 충돌 청크 제거 규칙을 정의합니다.
- [ ] 서로 다른 문서 간 내용 충돌 시 우선순위 정책을 정합니다.
- [ ] 근거 문서와 답변 문맥의 연결 가능성을 검증합니다.

## 4. 프롬프트 및 생성 품질

- [ ] 시스템 프롬프트와 태스크 프롬프트의 역할을 분리합니다.
- [ ] 답변 생성 프롬프트의 목표, 제약, 출력 형식을 명확히 정의합니다.
- [ ] 환각(Hallucination) 억제 규칙을 추가합니다.
- [ ] 정보 부족 시 안전하게 거절하거나 보류하는 문구를 정의합니다.
- [ ] Few-shot(소수 예시 학습), 규칙, Anchoring(기준 고정), Routing(질문 분기) 적용 여부를 검토합니다.
- [ ] Prompt 축소, 단순화, 구조화 실험 결과를 비교합니다.
- [ ] 검색 성공 후 생성 실패 케이스를 별도 관리합니다.
- [ ] 답변과 함께 근거, 출처, 신뢰 수준을 제공할지 정합니다.

## 5. 모델 선택 및 튜닝

- [ ] 기본 생성 모델 후보를 비교합니다.
- [ ] 성능, 비용, 지연시간, 운영 제약을 함께 평가합니다.
- [ ] SFT 적용 여부와 학습 데이터 기준을 정의합니다.
- [ ] DPO, KTO, SimPO, IPO, ORPO 등 선호도 학습 전략을 비교합니다.
- [ ] 튜닝 효과와 검색 개선 효과를 분리해서 해석합니다.
- [ ] 튜닝 모델의 회귀 케이스를 별도로 추적합니다.
- [ ] 운영 반영 전 안정성 검증 기준을 마련합니다.

## 6. 평가 체계

- [ ] 골든셋, 검증셋, 운영 샘플을 구분해 관리합니다.
- [ ] 정답률, 오답률, 누락률, 과답률, 검색 매칭률 등 핵심 KPI를 정의합니다.
- [ ] 자동 평가와 수동 검수를 함께 운영합니다.
- [ ] RAGAS, LLM Judge, 휴먼 리뷰의 역할을 구분합니다.
- [ ] 튜닝 전후와 프롬프트 변경 전후를 같은 기준으로 비교합니다.
- [ ] 카테고리별, 사용자 유형별, 실패 유형별 결과를 분리해 분석합니다.
- [ ] 평가 결과를 주기적으로 리포트합니다.

## 7. 오류 분석 및 개선 루프

- [ ] 실패 케이스를 검색 실패와 생성 실패로 구분합니다.
- [ ] 문서 미검색, 문서 오매칭, 근거 불일치, 답변 왜곡을 세분화합니다.
- [ ] 오류 케이스 분석 결과를 프롬프트, 검색, 데이터, 모델 개선으로 연결합니다.
- [ ] 반복적으로 발생하는 오류 패턴을 추적합니다.
- [ ] 개선 전후 동일 샘플로 회귀 여부를 점검합니다.
- [ ] 오류 분석 리포트를 표준 형식으로 남깁니다.

## 8. 보안 및 정책

- [ ] 접근권한이 있는 문서만 검색되도록 권한 정책을 적용합니다.
- [ ] 개인정보, 민감정보, 내부 전용 정보를 별도로 처리합니다.
- [ ] 프롬프트 인젝션 및 데이터 유출 대응 정책을 마련합니다.
- [ ] 금지 답변, 법적 제한, 안전 정책을 정의합니다.
- [ ] 로그에 남겨도 되는 정보와 제거해야 하는 정보를 구분합니다.

## 9. 개선활동 및 관측성

- [ ] 요청 수, 지연시간, 토큰 사용량, 실패율을 수집합니다.
- [ ] 검색 단계와 생성 단계의 병목을 구분해 측정합니다.
- [ ] 버전별 프롬프트, 모델, 인덱스 변경 이력을 기록합니다.
- [ ] 실험 로그, 결과, 회귀, 차주 계획을 같은 포맷으로 남깁니다.
- [ ] 장애 대응 절차와 롤백 기준을 정의합니다.
- [ ] 사용자 피드백을 수집하고 개선 작업과 연결합니다.

## 10. 개선 반영 전 최종 점검

- [ ] 대표 시나리오와 엣지 케이스를 모두 테스트합니다.
- [ ] 잘못된 근거 인용, 자신감 높은 오답, 정보 누락을 확인합니다.
- [ ] 비용과 지연시간이 목표 범위 안에 있는지 확인합니다.
- [ ] 보안, 정책, 감사 로그 요구사항을 충족하는지 점검합니다.
- [ ] 개선 반영 후 모니터링 항목과 책임자를 지정합니다.

## 핵심 개선활동 묶음

### 데이터 정비

- 데이터 수집, 정제, 중복 제거, 버전 관리
- 메타데이터 보강과 색인 품질 개선
- 골든셋 및 검증셋 관리

### 검색 개선

- Hybrid Search(하이브리드 검색), BM25, Dense Retrieval, Reranker 적용
- Top-K, Weight, Threshold 최적화
- Query Expansion 및 Query Rewrite 실험

### 생성 개선

- 프롬프트 구조 개선
- 환각(Hallucination) 억제 규칙 반영
- 근거 기반 답변 형식 정교화
- Few-shot(소수 예시 학습), Routing(질문 분기), Anchoring(기준 고정) 실험

### 평가 및 개선 루프

- 자동 평가와 수동 평가 병행
- 오류 유형 분류 및 리포트 작성
- 회귀 테스트와 반복 개선

### 개선활동 및 관리

- 로그, 비용, 지연시간 모니터링
- 버전 이력 관리
- 보안 정책과 접근권한 관리
- 사용자 피드백 반영

## 부록. 주요 용어 설명

### RAG

- Retrieval-Augmented Generation의 약자입니다.
- 모델이 답변 전에 외부 문서나 지식베이스에서 관련 정보를 검색해 함께 활용하는 방식입니다.
- 단순 LLM 답변보다 최신 정보 반영, 근거 제시, 내부 문서 기반 응답에 유리합니다.

### Lexical Search (어휘 기반 검색)

- 사용자가 입력한 단어와 문서에 포함된 단어를 직접 비교하는 전통적인 검색 방식입니다.
- 키워드가 정확히 일치하거나 유사한 철자를 포함한 문서를 잘 찾습니다.
- 대표적으로 BM25 같은 알고리즘이 사용됩니다.

### Vector Search (벡터 검색)

- 문장이나 문서를 임베딩 벡터로 변환한 뒤, 벡터 간 거리를 비교해 의미적으로 유사한 문서를 찾는 방식입니다.
- 정확히 같은 단어가 없어도 의미가 비슷하면 검색될 수 있습니다.
- 표현이 다양하거나 질문이 우회적으로 들어오는 환경에서 유리합니다.

### Hybrid Search (하이브리드 검색)

- Lexical Search와 Vector Search를 함께 사용하는 방식입니다.
- 키워드 일치와 의미 유사도를 동시에 반영하므로, 한 가지 방식만 쓸 때보다 검색 안정성이 높아지는 경우가 많습니다.
- 운영에서는 두 방식의 가중치 조정이 중요합니다.

### BM25

- 전통적인 정보검색 알고리즘 중 하나로, 문서 내 단어 빈도와 전체 문서 집합에서의 희소성을 함께 반영해 점수를 계산합니다.
- 검색 엔진에서 가장 널리 쓰이는 기본 랭킹 방식 중 하나입니다.
- 정확한 키워드 매칭이 중요한 업무 문서 검색에 여전히 강합니다.

### Dense Retrieval

- 질문과 문서를 모두 임베딩 벡터로 바꾼 뒤, 의미적 유사도로 문서를 검색하는 기법입니다.
- Vector Search의 핵심 검색 방식으로 이해하면 됩니다.
- 동의어, 문장 변형, 자연어 질문 처리에 강점이 있습니다.

### Reranker

- 1차 검색 결과를 다시 정렬하는 모델이나 알고리즘입니다.
- 1차 검색이 넓게 후보를 찾고, Reranker가 질문과 문서의 관련성을 더 정밀하게 판단해 상위 순위를 재조정합니다.
- 검색 정확도를 높일 수 있지만 추가 비용과 지연시간이 발생합니다.

### Top-K

- 검색 결과 중 상위 몇 개 문서를 사용할지 정하는 값입니다.
- 예를 들어 Top-K가 5면 검색 결과 상위 5개만 답변 생성에 사용합니다.
- 너무 작으면 필요한 근거를 놓칠 수 있고, 너무 크면 노이즈와 비용이 증가합니다.

### Score Threshold

- 검색 점수가 일정 기준 이상인 문서만 채택하는 임계값입니다.
- 낮게 잡으면 관련 없는 문서가 많이 들어오고, 높게 잡으면 필요한 문서까지 제외될 수 있습니다.
- Top-K와 함께 조정해야 합니다.

### Query Expansion

- 원래 질문에 연관 키워드나 동의어를 더해 검색 범위를 넓히는 기법입니다.
- 예를 들어 약어, 공식 용어, 유사 표현을 함께 검색하도록 바꿔 검색 누락을 줄입니다.

### Query Rewrite

- 사용자의 질문을 검색에 더 적합한 형태로 다시 쓰는 기법입니다.
- 길고 모호한 질문을 핵심 키워드 중심으로 정리하거나, 검색엔진이 잘 이해하는 구조로 바꾸는 데 사용합니다.

### Context Refinement (컨텍스트 정제)

- 검색된 문서를 그대로 넣지 않고, 답변에 필요한 부분만 정제해서 컨텍스트를 구성하는 작업입니다.
- 중복 문장 제거, 불필요한 메타데이터 제거, 충돌 정보 분리 등이 여기에 포함됩니다.

### 환각 (Hallucination)

- 모델이 실제 근거 없이 그럴듯하지만 틀린 내용을 만들어내는 현상입니다.
- RAG 환경에서도 검색은 성공했지만 생성 단계에서 왜곡이 생기면 환각이 발생할 수 있습니다.
- 근거 제시, 답변 제한 규칙, 안전한 거절 정책으로 완화합니다.

### Few-shot (소수 예시 학습)

- 프롬프트 안에 입력/출력 예시를 함께 넣어 원하는 답변 형식을 유도하는 방식입니다.
- 형식 일관성, 말투 통제, 판단 기준 전달에 유용합니다.

### Anchoring (기준 고정)

- 모델이 답변할 때 특정 기준 문장, 규칙, 근거 구조를 우선적으로 따르도록 고정하는 프롬프트 기법입니다.
- 답변 스타일과 판단 기준의 흔들림을 줄이는 데 도움이 됩니다.

### Routing (질문 분기)

- 질문 유형이나 난이도에 따라 서로 다른 프롬프트, 검색 전략, 모델, 처리 경로로 보내는 방식입니다.
- 예를 들어 FAQ성 질문은 빠른 경로로, 복합 분석 질문은 정교한 경로로 분기할 수 있습니다.

### SFT

- Supervised Fine-Tuning의 약자입니다.
- 정답이 포함된 학습 데이터로 모델을 지도학습 방식으로 추가 학습시키는 방법입니다.
- 특정 업무 말투, 응답 포맷, 도메인 지식 반영에 자주 쓰입니다.

### DPO

- Direct Preference Optimization의 약자입니다.
- 사람이 더 선호하는 응답과 덜 선호하는 응답의 비교 데이터를 이용해 모델 선호를 학습시키는 방식입니다.
- RLHF보다 구현이 단순한 편이라 최근 많이 사용됩니다.

### KTO

- Kahneman-Tversky Optimization의 약자입니다.
- 사람이 느끼는 이득과 손실의 비대칭성을 반영해 선호를 학습하려는 접근입니다.
- 안전성이나 사용성처럼 단순 정답률만으로 측정하기 어려운 영역에서 검토됩니다.

### SimPO

- Simple Preference Optimization의 약자입니다.
- 선호도 학습을 더 단순한 목적함수로 처리하려는 계열의 기법입니다.
- 구현 복잡도와 학습 안정성을 낮추려는 목적에서 비교됩니다.

### IPO

- Identity Preference Optimization의 약자입니다.
- 선호 학습 과정에서 기준 정책과의 관계를 더 안정적으로 유지하려는 방향의 방법론입니다.
- 실험 설계에 따라 DPO 계열과 함께 비교 대상이 됩니다.

### ORPO

- Odds Ratio Preference Optimization의 약자입니다.
- 선호 응답과 비선호 응답의 차이를 확률 비율 관점에서 학습하는 기법입니다.
- 선호도 학습 전략 비교 시 자주 함께 검토됩니다.

### RAGAS

- RAG 시스템 평가를 위한 대표적인 프레임워크입니다.
- 검색 적합성, 근거성, 정답 관련성 등 여러 평가 항목을 자동화해 측정할 수 있습니다.
- 휴먼 리뷰를 완전히 대체하기보다는 빠른 1차 품질 점검에 적합합니다.

### LLM Judge

- 다른 LLM을 평가자로 사용해 답변 품질을 채점하거나 비교하는 방식입니다.
- 대규모 자동 평가에 유용하지만, 평가 모델 자체의 편향과 일관성 문제를 함께 관리해야 합니다.

### 골든셋

- 정답과 기대 결과가 명확히 정의된 대표 평가 데이터셋입니다.
- 모델, 프롬프트, 검색 전략을 비교할 때 기준점 역할을 합니다.

### 검증셋

- 모델이나 시스템 변경 후 성능을 확인하기 위한 평가용 데이터셋입니다.
- 과적합을 줄이기 위해 학습 데이터와 분리해서 관리해야 합니다.

### 회귀 테스트

- 변경 이후 기존에 잘 되던 기능이나 응답 품질이 나빠지지 않았는지 확인하는 테스트입니다.
- LLM 시스템에서는 동일 질문 세트에 대해 이전 버전과 결과를 비교하는 형태로 자주 운영합니다.





